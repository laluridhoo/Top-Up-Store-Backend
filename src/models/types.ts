// The "@prisma/client" module has no exported member named 'Game'.
// Instead, import and re-export the correct type(s) as needed.
// If you want to re-export all types, you can do:
export * from "@prisma/client";
// Or, explicitly export a type that exists, e.g.:
// export type { User } from "@prisma/client";
